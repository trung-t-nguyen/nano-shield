# nano-guard
nano-guard is an ultra-lightweight (&lt;1KB), zero-dependency, frontend-focused RBAC (Role-Based Access Control) utility. It safely decodes JWTs in the browser and matches the user's roles against a centralized feature-to-role configuration map using an optional Module/App ID.
